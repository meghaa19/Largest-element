class Solution
{
public:
    int largest(vector<int> &arr, int n)
    {
        int largest_element=arr[0];
        int large=arr[0];
        
       for (int num=0;num<n;num++)
       {
           
           if(arr[num]>large)
           {
               large=arr[i];
               
               
           }
       }
       return large;
    }
};
